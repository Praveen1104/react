const initial = {
  money: 0,
};
export const MoneyReducer = ((state = initial, action) => {
  switch (action.type) {
    case "WITHDRAW":
      return state
    case "DEPOSIT":
      return state
    default:
      return state
  }
}):
